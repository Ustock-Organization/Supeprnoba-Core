# Supernoba Streamer 로그 로테이션 설정
# 위치: /etc/logrotate.d/supernoba-streamer
# 대상 서버: streamer (stock-streamer)

/var/log/supernoba/streamer/*.log {
    daily
    maxsize 200M
    rotate 3
    compress
    missingok
    notifempty
    create 0644 ec2-user ec2-user
    dateext
    dateformat -%Y%m%d-%H%M
    sharedscripts
    postrotate
        # systemd가 관리하므로 재시작 불필요
        true
    endscript
}
